[project]
name = "ocr-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "httpx",
    "python-dotenv",
    "uvicorn[standard]",
    "python-multipart",
    "paddlepaddle",
    "paddleocr",
    "pypdf>=6.14.2",
    "python-docx>=1.2.0",
]
